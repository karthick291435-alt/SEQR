document.querySelectorAll('.toast').forEach((toast)=>setTimeout(()=>toast.remove(),3500));
function startScanner(){const reader=document.getElementById('reader');if(!navigator.mediaDevices?.getUserMedia){reader.querySelector('p').textContent='Camera access is unavailable in this browser. Use the manual fallback.';return}navigator.mediaDevices.getUserMedia({video:{facingMode:'environment'}}).then(stream=>{reader.innerHTML='<video autoplay playsinline style="width:100%;border-radius:8px"></video><p class="muted">Point the camera at a QR code.</p>';reader.querySelector('video').srcObject=stream}).catch(()=>{reader.querySelector('p').textContent='Camera permission was denied. Use the manual fallback.'})}
